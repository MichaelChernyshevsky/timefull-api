tags:
- Package -> Timer
parameters:
  - in: body
    name: sendResetLinkRequest
    type: Object
    schema:
      $ref: "#/definitions/GetTime"
    
responses:
  "200":
    description: ""
    content:
      application/json:
        schema:
          type: "string"
  "404":
    description: ""

definitions:
  GetTime:
    type: object
    properties:
      userId:
        type: string
        example: "26"





tags:
- Package -> Timer
parameters:
  - in: body
    name: sendResetLinkRequest
    type: Object
    schema:
      $ref: "#/definitions/EditTimeHistory"
    
responses:
  "200":
    description: ""
    content:
      application/json:
        schema:
          type: "string"
  "404":
    description: ""

definitions:
  EditTimeHistory:
    type: object
    properties:
      userId:
        type: string
        example: "26"
      work:
        type: string
        example: "1"
      relax:
        type: string
        example: "1"
      





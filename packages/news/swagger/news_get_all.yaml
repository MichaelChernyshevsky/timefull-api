tags:
- News_package
parameters:
  - name: page
    in: page
    description: "page number"
    schema:
      type: string
      example: "1"
responses:
  "200":
    description: "A successful response"
    content:
      application/json:
        schema:
          type: "string"
  
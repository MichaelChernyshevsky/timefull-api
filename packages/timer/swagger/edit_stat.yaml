tags:
- Package -> Timer
parameters:
  - in: body
    name: sendResetLinkRequest
    type: Object
    schema:
      $ref: "#/definitions/EditTimeStat"
    
responses:
  "200":
    description: ""
    content:
      application/json:
        schema:
          type: "string"
  "404":
    description: ""

definitions:
  EditTimeStat:
    type: object
    properties:
      userId:
        type: string
        example: "26"
      timeWork:
        type: string
        example: "1"
      timeRelax:
        type: string
        example: "1"




